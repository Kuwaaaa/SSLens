# Archived VPS Deployment Draft

This folder preserves the temporary VPS deployment notes and templates created during the May 2026 Lumen server deployment discussion.

Contents here are draft material only:

- `lumen-server/` contains a Docker Compose sketch for the current Bun server container.
- `openresty/` contains OpenResty reverse-proxy configuration sketches.

Do not treat this folder as the canonical deployment guide or link it from the main project documentation until the deployment model is redesigned and reviewed.
